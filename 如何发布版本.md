# 🚀 如何发布新版本

## 📋 自动发布流程

工作流已配置完成，发布版本非常简单！

---

## 🎯 一键发布所有平台

只需要创建并推送一个版本标签：

```bash
# 1. 创建版本标签
git tag v1.0.0

# 2. 推送标签
git push origin v1.0.0
```

**就这么简单！** GitHub Actions 会自动：

1. ✅ 构建 Linux x86-64（RPM + DEB + AppImage）
2. ✅ 构建 Windows x86-64（ZIP）
3. ✅ 构建 macOS Intel（DMG）
4. ✅ 构建 macOS Apple Silicon（DMG）
5. ✅ 创建 GitHub Release
6. ✅ 上传所有构建产物
7. ✅ 生成详细的发布说明

---

## 📝 完整发布流程

### 步骤1: 更新版本号

在创建标签前，应该先更新这些文件中的版本号：

**1. `package.json`**
```json
{
  "version": "1.0.0"
}
```

**2. `src-tauri/Cargo.toml`**
```toml
[package]
version = "1.0.0"
```

**3. `src-tauri/tauri.conf.json`**
```json
{
  "version": "1.0.0"
}
```

### 步骤2: 更新 CHANGELOG（可选但推荐）

创建或更新 `CHANGELOG.md`：

```markdown
# Changelog

## [1.0.0] - 2025-10-22

### Added
- Initial release
- Cross-platform support (Linux, Windows, macOS)
- Modern React UI
- Real-time ANSI art preview
- Multiple output modes

### Changed
- Migrated from Electron to Tauri

### Fixed
- Icon transparency issues
- Dropdown menu visibility
```

### 步骤3: 提交更改

```bash
git add package.json src-tauri/Cargo.toml src-tauri/tauri.conf.json CHANGELOG.md
git commit -m "chore: Bump version to 1.0.0"
git push
```

### 步骤4: 创建并推送标签

```bash
# 创建带注释的标签（推荐）
git tag -a v1.0.0 -m "Release version 1.0.0

Features:
- Cross-platform Tauri desktop application
- Support for Linux, Windows, and macOS
- Modern React UI with TailwindCSS
- Real-time ANSI art preview
- Native file dialogs

Platforms:
- Linux x86-64 (RPM, DEB, AppImage)
- Windows x86-64
- macOS Intel
- macOS Apple Silicon
"

# 推送标签
git push origin v1.0.0
```

**或者创建简单标签**：
```bash
git tag v1.0.0
git push origin v1.0.0
```

---

## ⏱️ 等待构建完成

### 查看构建进度

1. 访问: https://github.com/ntbowen/chafa-gui/actions
2. 找到 "Build All Platforms" 工作流运行
3. 查看实时日志

### 预计时间

- **Linux**: ~5-8 分钟
- **Windows**: ~3-5 分钟
- **macOS Intel**: ~5-10 分钟
- **macOS Apple Silicon**: ~5-10 分钟

**总计**: ~15-20 分钟（并行执行）

---

## 📦 查看发布结果

### 发布完成后

访问: https://github.com/ntbowen/chafa-gui/releases

你会看到：

```
Release v1.0.0
==============

📦 Assets (6个文件):
  • Chafa GUI-1.0.0-1.x86_64.rpm          (3.4 MB)
  • chafa-gui_1.0.0_amd64.deb             (3.4 MB)
  • Chafa-GUI-1.0.0-x86_64.AppImage       (3.5 MB)
  • chafa-gui-windows-x64.zip             (6.1 MB)
  • chafa-gui-macos-intel.dmg             (~15 MB)
  • chafa-gui-macos-apple-silicon.dmg     (~15 MB)

📝 Release Notes:
  (自动生成的详细说明，包括安装指南、功能列表等)
```

---

## 🔧 手动触发构建（不创建发布）

如果只想测试构建而不发布：

1. 访问: https://github.com/ntbowen/chafa-gui/actions
2. 选择 "Build All Platforms" 工作流
3. 点击 "Run workflow" 按钮
4. 选择分支（main）
5. 点击绿色 "Run workflow" 按钮

**注意**: 手动触发不会创建 Release，只会生成 Artifacts

---

## 🗑️ 删除标签（如果出错）

如果需要删除错误的标签：

```bash
# 删除本地标签
git tag -d v1.0.0

# 删除远程标签
git push origin :refs/tags/v1.0.0

# 或使用新语法
git push origin --delete v1.0.0
```

然后可以重新创建正确的标签。

---

## 📋 版本号规范

推荐使用 [语义化版本](https://semver.org/lang/zh-CN/)：

```
v主版本号.次版本号.修订号

例如：
v1.0.0   - 首次正式发布
v1.0.1   - 修复bug
v1.1.0   - 添加新功能（向后兼容）
v2.0.0   - 重大更新（可能不兼容）
```

**格式要求**:
- 必须以 `v` 开头
- 使用三段式版本号
- 示例：`v1.0.0`, `v2.1.3`, `v0.1.0-beta`

---

## 🎯 发布检查清单

发布前确认：

- [ ] 更新了版本号（3个文件）
- [ ] 更新了 CHANGELOG.md
- [ ] 测试了主要功能
- [ ] 提交了所有更改
- [ ] 创建了正确的标签
- [ ] 推送了标签到 GitHub

发布后验证：

- [ ] 所有构建任务成功
- [ ] Release 已创建
- [ ] 所有6个文件已上传
- [ ] Release 说明正确
- [ ] 下载并测试至少一个平台的构建产物

---

## 💡 提示

### 预发布版本

创建预发布版本（测试版）：

```bash
git tag v1.0.0-beta.1
git push origin v1.0.0-beta.1
```

这会创建一个标记为 "Pre-release" 的发布。

### 编辑发布说明

发布创建后，可以在 GitHub 上手动编辑：

1. 访问 Releases 页面
2. 点击发布旁边的 "Edit" 按钮
3. 修改标题和说明
4. 更新或添加截图
5. 点击 "Update release"

### 修改现有发布

如果需要重新构建并替换文件：

1. 删除旧的 Release
2. 删除并重新创建标签
3. 重新推送标签

---

## 🎉 示例：完整的发布流程

```bash
# 1. 确保在 main 分支
git checkout main
git pull

# 2. 更新版本号
# 编辑 package.json, src-tauri/Cargo.toml, src-tauri/tauri.conf.json
vim package.json
vim src-tauri/Cargo.toml
vim src-tauri/tauri.conf.json

# 3. 更新 CHANGELOG
vim CHANGELOG.md

# 4. 提交更改
git add .
git commit -m "chore: Release v1.0.0"
git push

# 5. 创建标签
git tag -a v1.0.0 -m "Release v1.0.0"

# 6. 推送标签（触发自动构建和发布）
git push origin v1.0.0

# 7. 访问 Actions 页面查看构建进度
echo "Check: https://github.com/ntbowen/chafa-gui/actions"

# 8. 等待完成后访问 Releases 页面
echo "Release: https://github.com/ntbowen/chafa-gui/releases"
```

---

## 🚀 总结

**发布就是这么简单**：

```bash
git tag v1.0.0
git push origin v1.0.0
```

GitHub Actions 会自动完成剩下的所有工作，包括：
- 构建所有平台
- 创建 Release
- 上传所有文件
- 生成发布说明

**15-20分钟后，你的跨平台应用就发布完成了！** 🎉
